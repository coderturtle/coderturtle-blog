# Gremlin Radar: coderturtle.io

Automation opportunities captured at session closeout — repeated manual steps,
corrections, or workflows that could become a Claude Code hook, skill, subagent,
or Hekton gremlin (see ~/hekton/agents/gremlin-model.md). Each entry is a candidate,
not a commitment — expand into a full writeup (problem / signal it watches / where
it would live / output / build priority) when ready to build. The central ledger is
`~/hekton/state/gremlin-radar.jsonl`.

## 2026-07-18 - Built the real Workshops page; fixed two other repos' broken custom domains as a precondition

- **Idea:** Confirmed which of two similarly-named things is the real editorial-review tool (blog-factory-lab's Editor Gremlin critique role, not the Editorial Gremlin pipeline dispatcher) - worth a naming clarification in the gremlin-model docs to stop this recurring
- **Suggested form:** hook
- **Status:** new

