# Next Actions

- [x] Fill local Terraform tfvars for `infra/aws-static-site/terraform/` after confirming Route 53 hosted zone ID and bucket name — hosted zone `Z3DLQAZ6G5LV63` and bucket `coderturtle-io-static-prod-600059206606` both resolved via read-only AWS calls (2026-07-02), no human input needed; recorded in `infra/aws-static-site/deploy-manifest.yaml` and `docs/architecture.md`.
- [x] Run Infrastructure Gremlin preflight and Terraform plan for the AWS static-site stack — done on `infra/aws-static-site-cutover` branch (2026-07-02): 17 to add, 0 to change, 0 to destroy, classifies GREEN, Well-Architected advisory WARN (same profile as the sibling `hekton.theagentictekton.com` site's first plan). Full summary in `docs/decisions.md`.
- [x] Human-review and apply the AWS static-site Terraform plan — applied 2026-07-03 (human-run, not by any agent). Hit and resolved two real conflicts against a pre-existing live 2020 CloudFront distribution that already held the domain's DNS aliases (see `docs/decisions.md`). New stack live: bucket `coderturtle-io-static-prod-600059206606`, distribution `EVFZ1SPLK9PZ1`, DNS repointed and verified.
- [x] Configure GitHub OIDC role and repository variables — role added via Terraform (`github-oidc.tf`, converging on agentic-tekton's pattern, see `docs/decisions.md`), applied 2026-07-03. All four repo variables (`AWS_REGION`, `AWS_DEPLOY_ROLE_ARN`, `S3_BUCKET`, `CLOUDFRONT_DISTRIBUTION_ID`) set via `gh variable set` 2026-07-03. Deploy workflow will perform its first real deploy on the next merge to `main`.
- [x] Decommission the old 2020 CloudFront distribution (`E1L5J5X47UHN2S`) and its public S3-website bucket (`coderturtle.io`) — backed up locally (`.old-site-backup-2020/`, gitignored, 60 objects/1.2MB) before deletion since the bucket held a completely different, older site not otherwise preserved anywhere. Human-run: disabled the distribution, waited for propagation, deleted it, emptied and deleted the bucket (2026-07-03). Confirmed gone via `aws cloudfront get-distribution`/`aws s3api head-bucket` (both 404/NoSuchDistribution).
- [x] Verify the first real deploy once PR #2/#3 merged — confirmed: `deploy-aws-static-site.yml` ran successfully on the PR #2 merge push, `curl https://coderturtle.io/` returns `200` with the actual new site content, CloudFront invalidation ran. A subsequent client-side report of "still seeing the old site" traced to local browser cache, not a real server-side issue — confirmed via `curl` and DNS lookups showing correct state throughout (2026-07-03).
- [x] Re-investigate a second, identically-worded "`/projects/` shows the old coming-soon page" report — cache-busted `curl` (apex + `www`) and `dig` again show only the current, correct build served from the new CloudFront distribution; same browser-cache root cause as the 2026-07-03 report, nothing server-side to fix (2026-07-04).
- [x] Fold the old site's footer quote ("Innovation is seeing what everybody has seen and thinking what nobody has thought") into the current brand — added as a sitewide footer tagline in `src/layouts/BaseLayout.astro`/`src/styles/global.css`, matching its placement on the old site rather than the homepage's separate cranky-quote rotation (2026-07-04).
- [ ] Review `astro.config.mjs` site/base settings against the intended `coderturtle.io` production domain.
- [x] Remove tracked `.DS_Store` and `.github/.DS_Store` in a dedicated hygiene commit — untracked in `09ff31f` (2026-07-02); already covered by `.gitignore`, files kept locally.
- [x] Decide when to retire the current GitHub Pages workflow after AWS deployment is verified — user turned off the GitHub Pages workflow (2026-07-03) once the AWS path was confirmed serving production traffic. `.github/workflows/deploy.yml` itself is still present but disabled; deleting the file outright is a small later cleanup, not urgent.
- [x] Review npm audit output and plan dependency updates in a dedicated maintenance branch — non-breaking `npm audit fix` resolved 21/26 findings (26 -> 5, all remaining require a breaking astro@5->7 major bump); build verified green; done on `chore/npm-audit-fixes`, fast-forward merged into the product branch and deleted (2026-07-02).
- [ ] Plan a dedicated astro@5->7 upgrade effort to close the remaining 5 npm audit findings (4 low, 1 high) once there's time to test for breaking changes across the site.
- [ ] Review and optionally sync repo-local `mind-palace/` files into the live vault after backup.
- [ ] Split real Labs and Workshops pages out of the homepage once source content exists.
- [ ] Decide whether `/enter/` should remain the long-term content hub or become a temporary bridge until Labs and Workshops pages exist.
- [ ] Human-review the turtle quote set and decide whether to expand it into a reusable Coderturtle voice library.
- [x] Review `/enter/` as the intentional home for Hekton, Agentic Tekton, Gremlins, and wider ecosystem references — superseded: ecosystem content now lives on its own `/ecosystem/` page instead (2026-07-02), per explicit user request to keep it off the main page.
- [x] Human-review the dark-first "gateway to the future" gateway direction — user confirmed the gateway is good (2026-07-02).
- [x] Address `/enter/` still not landing for the user — replaced with an interactive workshop-floor scene (master turtle + labs/projects/workshops turtle stations) and moved Projects/Ecosystem content off the page entirely (2026-07-02).
- [x] Confirm Hekton's real public URL — user confirmed `https://hekton.theagentictekton.com` (2026-07-02); now a real outbound link on `/ecosystem/`.
- [x] Confirm Gremlins' real public URL (or confirm it doesn't exist yet) — asked the user directly, no response; ratified "coming soon" as a standing recorded decision rather than an unowned default (2026-07-02, see `docs/decisions.md`). Reversible: swap `href: null` for a real URL in `src/pages/ecosystem.astro` whenever one exists.
- [x] Address `/enter/` "still feels 2 dimensional" — added pointer-tracked 3D tilt on worker stations, a real bus/drop-line connector diagram with hover-activated glow pulse, idle pulsing status pips, and drag interaction on the master turtle (2026-07-02). Not yet re-confirmed with the user.
- [ ] Confirm the dark/glow palette/mood choice explicitly for `/enter/` and beyond — the user didn't respond to the original clarifying question; the gateway itself is confirmed good, worth a final check now that `/enter/` has also changed substantially.
- [x] Supply real illustrated artwork for the worker turtle stations — user supplied `lab-turtle.png`/`project-turtle.png`/`workshop-turtle.png` (2026-07-02), now wired into `/enter/` via `astro:assets` (`src/assets/turtles/`), replacing the reused-Coderturtle-portrait placeholder for all three stations.
- [x] Once real Labs and Workshops pages exist, link the labs-turtle/workshops-turtle stations on `/enter/` to them (currently non-linking buttons with a "still being built" readout). **Workshops half done 2026-07-18**: `/workshops/` built for real (content collection, 4 real workshops with tags/status, client-side tag filter), `/enter/`'s workshops-turtle now links there, `Workshops` added to the sitewide nav. **Labs still pending** — no real Labs page exists yet, that station still reads "still being built."
- [ ] Consider commissioning additional hand-drawn Coderturtle illustration variants (the design review found the current character art underused; this pass reused existing assets only).
- [x] Confirm or override the default narrative direction — user confirmed documentary, kept separate from `hekton-field-journal`/`tekton-chronicle`'s gremlin fiction (2026-07-02).
- [x] Phase 2: build the visual build-log component library — shipped `TerminalBlock`, `DiffBlock`, `Screenshot`, `BeforeAfter` in `src/components/log/` (2026-07-02), demonstrated in a new real entry (`0002-the-dark-reset.mdx`). `LogMeta`/`LogEntry` were deliberately not extracted into separate components — the project-detail timeline and entry page already render this inline and work correctly, so extracting was judged lower-value than the four content components.
- [x] Phase 3: build the agent-facing interface — shipped `/llms.txt`, `/projects/index.json`, `/projects/[slug].json` (2026-07-02), all build-time generated. Verified via `npm run preview` (real static output), not just `npm run dev`, after finding a dev-server-only trailing-slash quirk that doesn't affect the actual static build.
- [x] Set a global voice for Coderturtle content — `docs/voice-guide.md` written (2026-07-02): cranky battle-scarred engineer persona + Coding Horror dry-humor reference. Applied immediately in `0003-cogs-tilt-and-a-terminal-that-talks-to-agents.mdx`; `0001-vibing-with-chatgpt.mdx` deliberately left unrewritten (predates the guide, kept as an honest record).
- [x] Phase 4: about/manifesto tie-in — added a new `/about/` section ("An agent does some of the typing. The turtle still owns it.") stating the documentary/human-agent-collaboration premise directly (2026-07-02). Closes out the original four-phase content-architecture plan.
- [ ] Populate curated `changeRefs` on entries once `.hekton/change-log.yaml` has entries beyond the original CHG-0001 adoption record worth citing.
- [ ] Human-review `/llms.txt` and the JSON endpoints live.
- [ ] Add real project entries beyond the single migrated "Coderturtle.io" project once other real projects have material worth logging.
- [x] Route new build-log screenshots/imagery through `src/assets` (astro:assets/sharp) rather than `public/` — done for all four demo screenshots across `0002`/`0003` (`src/assets/logs/coderturtle-io/`); still applies to any future build-log imagery.
- [ ] Human-review the build-log component library, the new voice, and the About page tie-in live — terminal blocks, diff coloring, screenshot zoom, before/after slider, and whether the Coding Horror tone lands.
- [ ] Human-review the new worker-turtle art on `/enter/` — confirm the illustration-style contrast between the master turtle and the three worker turtles (hand-drawn vs. clean flat-vector) reads as intentional "senior supervising juniors" rather than a mismatch. If the master turtle's own portrait ever gets restyled, revisit whether these three should match.
- [ ] **Human-review `/workshops/` live, in a real browser** (2026-07-18 build) — no browser-automation tool was available this session, so the new page/tag-filter/nav wiring were only verified via `npm run build` + generated-HTML inspection, not an actual rendered/interactive check (dark+light theme, mobile width, tag-filter click behavior, focus states). This project's own established practice is a real browser check before calling a UI change done; that step is still outstanding here.
- [ ] Build a real `/labs/` page — `/enter/`'s labs-turtle station still reads "still being built" (only the workshops-turtle half of this long-open item was closed 2026-07-18).
- [ ] Add a 5th `workshops` entry whenever the next workshop launches — same pattern as `src/content/workshops/*.md`, and consider whether the onboarding checklist for a new workshop (in `agentic-infra-lab`'s `github-pages-dns` pattern docs) should gain a step pointing here, so this page doesn't silently fall behind the real workshop roster the way `/workshops/` itself did for months.
