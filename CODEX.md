# CODEX.md - Coderturtle.io

Codex agents working in this repo must follow `AGENTS.md` and `~/hekton/AGENTS.md`.

Key constraints:

- Use `rg`/`rg --files` for discovery where practical.
- Read `.hekton/project.yaml` and nearby docs before editing.
- Keep Hekton adoption work separate from app refactors or dependency upgrades.
- Use targeted staging paths only.
- Do not mutate the live vault without explicit user authorization.
- Record validation and next actions before handoff.
