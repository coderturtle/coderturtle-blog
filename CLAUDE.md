# CLAUDE.md - Coderturtle.io

Claude agents working in this repo must follow `AGENTS.md` and `~/hekton/AGENTS.md`.

Key constraints:

- Read `.hekton/project.yaml` before structural changes.
- Keep Hekton adoption metadata separate from product changes.
- Do not mutate the live vault without explicit user authorization.
- Record material decisions in `docs/decisions.md`.
- End material sessions with a `docs/session-log.md` entry and validation status.
